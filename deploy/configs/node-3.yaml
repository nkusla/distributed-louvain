machine_id: "node-3"
port: 8080
is_coordinator: false
coordinator: "coordinator:8080"

algorithm:
  max_iterations: 100
  timeout: "300s"
  grace_period: "2s"

actors:
  partitions: 4
  aggregators: 2

network:
  peers:
    - id: "node-1"
      address: "node-1:8080"
    - id: "node-2"
      address: "node-2:8080"
